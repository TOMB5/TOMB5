#ifndef GTEMATH_H
#define GTEMATH_H

#include "STYPES.H"

extern SVECTOR MVMVA(int x, int y, int z, char shift);
extern SVECTOR MVMVA(int x, int y, int z);
extern void RTPT(int tx, int ty, int tz, SVECTOR* v);
extern unsigned int SXY[3];
extern unsigned short SZ[4];

#endif